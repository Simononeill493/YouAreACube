namespace IAmACube
{
    public enum CoordinateMode
    {
        Undefined,
        Absolute,
        ParentPixel,
        ParentPercentage,
        ParentAbsolute,
        VisualParentPixel
    }
}
